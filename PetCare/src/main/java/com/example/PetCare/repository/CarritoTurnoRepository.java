package com.example.PetCare.repository;

import com.example.PetCare.model.CarritoTurno;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarritoTurnoRepository extends JpaRepository<CarritoTurno, Integer> {
}
